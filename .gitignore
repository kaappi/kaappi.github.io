/site/
__pycache__/
# Fetched at build time from the matching kaappi release (scripts/fetch-wasm.sh);
# not committed, so release binaries don't bloat history. Run the script once
# before `mkdocs serve` on a fresh clone.
/docs/wasm/kaappi.wasm
# Same pattern for the embedded book PDF (scripts/fetch-book.sh), pinned by
# book_version in mkdocs.yml.
/docs/assets/kaappi-book.pdf
# LaTeX aux files for the cheatsheet. The built PDF (docs/assets/
# kaappi-cheatsheet.pdf) IS committed — small-asset pattern, unlike the
# fetched book PDF above. Rebuild with `make -C cheatsheet`.
/cheatsheet/build/
# Claude Code local artifacts (shared config such as .claude/launch.json stays
# tracked). Previously only excluded via this clone's .git/info/exclude.
.claude/worktrees/
.claude/settings.local.json
